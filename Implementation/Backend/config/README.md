# Backend — Config

Environment- and deployment-specific configuration for the backend (e.g. database connection strings, secrets, per-environment settings) — see [Arch/ARCH-04](../../../Arch/ARCH-04-deployment.md) for the deployment layout this supports.

Not started yet.
