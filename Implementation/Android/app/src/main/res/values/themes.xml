<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. Compose owns the actual color scheme (see ui/theme/Theme.kt);
         this XML theme only needs to exist so the manifest/activity has a valid base. -->
    <style name="Theme.MedVerify" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
